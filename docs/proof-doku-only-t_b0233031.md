# Beleg-Datei (Karte t_b0233031, AK2)

Diese Datei existiert nur auf dem Beweis-Branch `proof/t_b0233031-b-doku-only`
und wird nicht gemergt.

Zweck: ein Diff, der ausschliesslich aus Dokumentation besteht (`docs/**`), damit
der `changes`-Job ihn als `code=false` klassifiziert und die fuenf schweren Jobs
uebersprungen werden. Der Aggregat-Job `all-green` muss dabei **gruen** werden —
das ist der legitime Skip-Fall, den er vom gefaehrlichen unterscheiden koennen
muss.
