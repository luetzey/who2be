# packages/models

Geteilte Pydantic-Models fuer Who2Be (Persona, Playbook, Versionierung,
Persona-Playbook-Verknuepfung).

Platzhalter — befuellt in Phase 1. Wird von `apps/api` und `apps/mcp` importiert,
damit Schemas nicht dupliziert werden.
