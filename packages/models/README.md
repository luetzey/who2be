# packages/models

Geteilte Pydantic-Models fuer Who2Be (Persona, Playbook, Versionierung,
Persona-Playbook-Verknuepfung).

Phase 0: nur Paket-Geruest (`who2be_models`). Die Domain-Models folgen in
Phase 1. Wird von `apps/api` und `apps/mcp` importiert, damit Schemas nicht
dupliziert werden.
