# Plan: #499 — GoTrue-Pin auf v2.196.0 in allen drei Compose-Stacks (W1 von #435)

**Karte:** t_1c6dc7e5 · **Branch:** `wt/i499-gotrue-bump` · **Issue:** luetzey/who2be#499

## Outcome

Alle drei Compose-Stacks laufen auf `supabase/gotrue:v2.196.0`; in den beiden
Stacks mit MFA-Block ist der WebAuthn-Faktor serverseitig verfuegbar (beide
Faktor-Schalter explizit `true`, drei Pflicht-RP-Variablen gesetzt); die
Env-Doku nennt die neue Version. Die Passkey-UI ist **out of scope** (W2).

## Gegenprobe zum Breaking-Change-Check (2026-09-08, `supabase/auth` @ `0907af9`)

Der Check wird nicht wiederholt, nur gegengeprueft — frueheren Laeufen fehlte
der Upstream-Zugriff. Heute gemessen (`gh api repos/supabase/auth/...`):

| Frage | Messung | Folge |
|---|---|---|
| Hat sich der Upstream seit `0907af9` bewegt? | `compare 0907af9...v2.196.0` → `status: behind`, `ahead_by: 0`, `behind_by: 38`. `0907af9` ist vom **2026-09-03**, der Tag `v2.196.0` vom **2026-08-18**. | Der Check las einen Stand **nach** der Zielversion. Es gibt keine Migration in `v2.196.0`, die er nicht gesehen hat. Die Aussagen des Checks halten fuer `v2.196.0` ohne Einschraenkung. |
| Migrationen `v2.158.1` → `v2.196.0` | 52 → 70 Dateien = **18** neue, darunter `20241009103726_add_web_authn`, `20250925093508_add_last_webauthn_challenge_data`, `20260302000000_add_passkeys`. | Die drei im Issue genannten WebAuthn-Migrationen sind in `v2.196.0` enthalten. (Das Issue sagt „23 Migrationen"; das ist die Zahl bis `0907af9`, also inkl. der 5 aus `v2.197.0`.) |
| Warn-statt-Error bei unvollstaendiger WebAuthn-Config? | `configuration.go` @ `v2.196.0`: `if c.Passkey.Enabled \|\| c.MFA.WebAuthn.EnrollEnabled \|\| c.MFA.WebAuthn.VerifyEnabled { if err := c.WebAuthn.Validate(); err != nil { logrus…Warn(…) } }` | Bestaetigt. Eine fehlende RP-Variable bricht den Start **nicht** mehr ab. |
| Variablen-Praefixe | `WebAuthnConfiguration`: `RPID` mit `envconfig:"RP_ID"`, `RPDisplayName`, `RPOrigins` (split_words) → `GOTRUE_WEBAUTHN_RP_ID/_RP_DISPLAY_NAME/_RP_ORIGINS`. Faktor-Schalter: `MFAConfiguration.WebAuthn MFAFactorTypeConfiguration` → `GOTRUE_MFA_WEB_AUTHN_ENROLL_ENABLED/_VERIFY_ENABLED`. | Beide Praefixe wie im Issue. Bestaetigt. |
| Defaults der Faktor-Schalter | `MFAFactorTypeConfiguration`: `default:"false"` fuer Enroll **und** Verify; `TOTPFactorTypeConfiguration` hat eigenen Typ mit `default:"true"`. | Explizites Setzen ist Pflicht. Bestaetigt. |

**Warum nicht `v2.197.0`?** Die aktuellste stabile Version zum heutigen Tag
waere `v2.197.0` (`v2.198.0` nur als RC). Sie bringt fuenf weitere Migrationen
(SCIM-Users/-Tokens, Recovery-Codes, One-Time-Token-Expiry), die der
Breaking-Change-Check **nicht** abdeckt — `0907af9` liegt vor ihnen. Der
Auftrag nennt `v2.196.0` namentlich, und das ist die hoechste Version, die der
Check traegt. Kein Grund, ueber die gepruefte Grenze hinauszugehen.

## Schritte

1. **Pin heben** in `docker-compose.yml:63`, `deploy/hetzner/supabase/docker-compose.yml:63`, `deploy/dokploy/docker-compose.yml:81` → `supabase/gotrue:v2.196.0`.
2. **MFA-/WebAuthn-Block** in den zwei Stacks mit MFA-Block (lokal + hetzner):
   `GOTRUE_MFA_WEB_AUTHN_ENROLL_ENABLED` / `_VERIFY_ENABLED` explizit `true`,
   plus `GOTRUE_WEBAUTHN_RP_ID` / `_RP_DISPLAY_NAME` / `_RP_ORIGINS`.
   Die veralteten Kommentare („v2.158.1 kennt kein Top-Level `GOTRUE_MFA_ENABLED`")
   werden mitgezogen. **dokploy bekommt nur den Pin** (Pin-Symmetrie, nicht
   Konfigurations-Symmetrie — Weiche 5 des Issues).
3. **AK 4 absichern** (die Entscheidung des Owners lautet „CI ist die
   Verifikationsinstanz", deckt AK 4 aber nicht): `scripts/smoke.sh` bekommt
   Check 9 — das **laufende** `auth`-Image traegt den gepinnten Tag, und
   `docker compose logs auth` enthaelt keinen Migrations-/Fatal-Fehler, aber die
   Startzeile. Damit wird AK 4 zur Assertion statt zum Log-Blick. Gewaehlter
   Weg von den zwei zulaessigen: **Assertion**, weil ein Log-Blick pro Lauf
   wiederholt werden muesste und nichts gegen Regression sichert.
4. **Doku nachziehen:** `.env.example`, `deploy/hetzner/.env.example`,
   `deploy/hetzner/RUNBOOK.md`, `docs/mfa-admin.md:77`, `CHANGELOG.md`
   (Unreleased), `.claude/context/DECISIONS.md` (Zielversion).
5. **Verifizieren:** `grep -rn "supabase/gotrue" --include="*.yml" .` → drei
   Treffer, gleiche Version; `bash -n scripts/smoke.sh`; `docker compose config`
   ist lokal **nicht** fahrbar (kein Docker-Daemon, `which docker` leer) →
   YAML-Parse per Python-`yaml` als Ersatz; Rest verifiziert CI.
6. **PR oeffnen**, CI gruen abwarten. Merge/Push auf main sind gesperrt.

## Constraints

- Keine Aenderung unter `apps/web/**`; keine TOTP-Variable wird entfernt oder umbenannt.
- `deploy/dokploy/docker-compose.cloud.yml` bleibt unberuehrt (kein GoTrue-Pin).
- Lokal kein Docker-Daemon — jede daemon-gebundene Verifikation laeuft in CI.
