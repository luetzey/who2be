# BlockNote (@blocknote/mantine) zieht @mantine/core, dessen exakter
# @mantine/hooks-Peer von npms strikter Peer-Aufloesung abgelehnt wird.
# legacy-peer-deps haelt install UND ci (CI, SessionStart-Hook) konsistent.
legacy-peer-deps=true
