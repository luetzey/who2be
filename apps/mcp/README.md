# apps/mcp

FastMCP-Server fuer Who2Be.

Platzhalter — befuellt in Phase 2 (MCP): Tools `get_persona`, `list_playbooks`
(Tag-/Trigger-Filter), `fetch_playbook`.
Importiert geteilte Pydantic-Models aus `packages/models/`.
